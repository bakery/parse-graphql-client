import index from '../../src/parse-graphql';

describe('index', () => {
  describe('Greet function', () => {
    beforeEach(() => {
      // spy(index, 'greet');
      // index.greet();
    });

    it('should have been run once', () => {
      // expect(index.greet).to.have.been.calledOnce;
    });

    it('should have always returned hello', () => {
      // expect(index.greet).to.have.always.returned('hello');
    });
  });
});
